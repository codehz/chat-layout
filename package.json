{
  "name": "chat-layout",
  "private": true,
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "tsdown": "^0.21.7",
    "typescript": "^5.7.2"
  },
  "dependencies": {},
  "scripts": {
    "dist": "bun run scripts/dist.ts",
    "example": "cd example && bun run build.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
